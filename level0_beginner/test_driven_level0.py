# ============================================
# LEVEL 0: TEST-DRIVEN DEVELOPMENT (TDD)
# ============================================
# Тесты уже написаны — напишите код, который их пройдёт!
# Время выполнения: 30-40 минут
# ============================================

"""
🧪 ЧТО ТАКОЕ TDD (Test-Driven Development)?

TDD = Разработка через тестирование

Подход:
1. Сначала пишутся ТЕСТЫ (что должно работать)
2. Потом пишется КОД (чтобы пройти тесты)
3. Зелёные тесты ✅ = код работает!

Это как в видео-игре: задание известно, нужно его выполнить!

💡 Если забыли синтаксис циклов/функций — смотрите primm_level0.py или solutions!
"""

# ============================================
# КАК РАБОТАТЬ С ЭТИМ ФАЙЛОМ
# ============================================

print("""
📚 ИНСТРУКЦИЯ:

1. Прочитайте описание функции
2. Посмотрите на тесты (что ожидается)
3. Напишите код функции
4. Запустите файл
5. Если все тесты ✅ зелёные — переходите к следующей задаче
6. Если есть ❌ красные — исправьте код

💡 ПОДСКАЗКА: Начните с простой версии, потом улучшайте!
""")

# ============================================
# ЗАДАЧА 1: ПРИВЕТСТВИЕ
# ============================================

def greet(name):
    """
    Функция приветствия

    Параметры:
        name (str): Имя человека

    Возвращает:
        str: Строку вида "Привет, [имя]!"

    Примеры:
        greet("Алиса") → "Привет, Алиса!"
        greet("Боб") → "Привет, Боб!"
    """
    # TODO: Напишите код здесь
    pass  # удалите эту строку и напишите свой код


# Тесты для задачи 1
def test_greet():
    print("\n🧪 Тесты для greet():")

    # Тест 1
    result = greet("Алиса")
    expected = "Привет, Алиса!"
    if result == expected:
        print(f"✅ Тест 1 пройден: greet('Алиса') = '{result}'")
    else:
        print(f"❌ Тест 1 НЕ пройден: ожидалось '{expected}', получено '{result}'")

    # Тест 2
    result = greet("Боб")
    expected = "Привет, Боб!"
    if result == expected:
        print(f"✅ Тест 2 пройден: greet('Боб') = '{result}'")
    else:
        print(f"❌ Тест 2 НЕ пройден: ожидалось '{expected}', получено '{result}'")

    # Тест 3
    result = greet("Python")
    expected = "Привет, Python!"
    if result == expected:
        print(f"✅ Тест 3 пройден: greet('Python') = '{result}'")
    else:
        print(f"❌ Тест 3 НЕ пройден: ожидалось '{expected}', получено '{result}'")


# ============================================
# ЗАДАЧА 2: ПОВТОРЕНИЕ СТРОКИ
# ============================================

def repeat_text(text, times):
    """
    Повторить текст несколько раз

    Параметры:
        text (str): Текст для повторения
        times (int): Сколько раз повторить

    Возвращает:
        str: Текст, повторённый нужное количество раз

    Примеры:
        repeat_text("Ха", 3) → "ХаХаХа"
        repeat_text("Го", 2) → "ГоГо"
    """
    # TODO: Напишите код здесь
    pass


# Тесты для задачи 2
def test_repeat_text():
    print("\n🧪 Тесты для repeat_text():")

    tests = [
        (("Ха", 3), "ХаХаХа"),
        (("Го", 2), "ГоГо"),
        (("Привет", 1), "Привет"),
        (("*", 5), "*****"),
    ]

    for i, (args, expected) in enumerate(tests, 1):
        result = repeat_text(*args)
        if result == expected:
            print(f"✅ Тест {i} пройден: repeat_text{args} = '{result}'")
        else:
            print(f"❌ Тест {i} НЕ пройден: ожидалось '{expected}', получено '{result}'")


# ============================================
# ЗАДАЧА 3: ЧЁТНОЕ ИЛИ НЕЧЁТНОЕ
# ============================================

def is_even(number):
    """
    Проверить, является ли число чётным

    Параметры:
        number (int): Число для проверки

    Возвращает:
        bool: True если чётное, False если нечётное

    Примеры:
        is_even(4) → True
        is_even(7) → False
        is_even(0) → True
    """
    # TODO: Напишите код здесь
    # ПОДСКАЗКА: используйте оператор % (остаток от деления)
    # Чётное число: number % 2 == 0
    pass


# Тесты для задачи 3
def test_is_even():
    print("\n🧪 Тесты для is_even():")

    tests = [
        (4, True),
        (7, False),
        (0, True),
        (1, False),
        (100, True),
        (99, False),
    ]

    for number, expected in tests:
        result = is_even(number)
        if result == expected:
            print(f"✅ Тест пройден: is_even({number}) = {result}")
        else:
            print(f"❌ Тест НЕ пройден: is_even({number}) ожидалось {expected}, получено {result}")


# ============================================
# ЗАДАЧА 4: СУММА ЧИСЕЛ ОТ 1 ДО N
# ============================================

def sum_to_n(n):
    """
    Вычислить сумму всех чисел от 1 до n

    Параметры:
        n (int): Число до которого считать

    Возвращает:
        int: Сумма чисел

    Примеры:
        sum_to_n(5) → 15  (1+2+3+4+5)
        sum_to_n(3) → 6   (1+2+3)
        sum_to_n(1) → 1
    """
    # TODO: Напишите код здесь
    # ПОДСКАЗКА: используйте цикл for и переменную для накопления суммы
    pass


# Тесты для задачи 4
def test_sum_to_n():
    print("\n🧪 Тесты для sum_to_n():")

    tests = [
        (5, 15),   # 1+2+3+4+5 = 15
        (3, 6),    # 1+2+3 = 6
        (1, 1),    # 1 = 1
        (10, 55),  # 1+2+...+10 = 55
    ]

    for n, expected in tests:
        result = sum_to_n(n)
        if result == expected:
            print(f"✅ Тест пройден: sum_to_n({n}) = {result}")
        else:
            print(f"❌ Тест НЕ пройден: sum_to_n({n}) ожидалось {expected}, получено {result}")


# ============================================
# ЗАДАЧА 5: СТРОИМ ЛЕСТНИЦУ
# ============================================

def build_stairs(height):
    """
    Построить лестницу из звёздочек

    Параметры:
        height (int): Высота лестницы

    Возвращает:
        str: Строка с лестницей (каждая ступень на новой строке)

    Примеры:
        build_stairs(3) →
        "*
         **
         ***"

        build_stairs(1) → "*"
    """
    # TODO: Напишите код здесь
    # ПОДСКАЗКА: используйте цикл и \n для новой строки
    pass


# Тесты для задачи 5
def test_build_stairs():
    print("\n🧪 Тесты для build_stairs():")

    # Тест 1
    result = build_stairs(3)
    expected = "*\n**\n***"
    if result == expected:
        print("✅ Тест 1 пройден: build_stairs(3)")
        print(result)
    else:
        print(f"❌ Тест 1 НЕ пройден")
        print(f"Ожидалось:\n{expected}")
        print(f"Получено:\n{result}")

    # Тест 2
    result = build_stairs(1)
    expected = "*"
    if result == expected:
        print("\n✅ Тест 2 пройден: build_stairs(1)")
    else:
        print(f"\n❌ Тест 2 НЕ пройден: ожидалось '{expected}', получено '{result}'")


# ============================================
# БОНУСНАЯ ЗАДАЧА: КАЛЬКУЛЯТОР
# ============================================

def calculate(a, b, operation):
    """
    Простой калькулятор

    Параметры:
        a (int): Первое число
        b (int): Второе число
        operation (str): Операция: "+", "-", "*", "/"

    Возвращает:
        float или int: Результат операции

    Примеры:
        calculate(5, 3, "+") → 8
        calculate(10, 2, "-") → 8
        calculate(4, 3, "*") → 12
        calculate(10, 2, "/") → 5.0
    """
    # TODO: Напишите код здесь
    # ПОДСКАЗКА: используйте if/elif для проверки operation
    pass


# Тесты для бонусной задачи
def test_calculate():
    print("\n🧪 Тесты для calculate() (БОНУС):")

    tests = [
        ((5, 3, "+"), 8),
        ((10, 2, "-"), 8),
        ((4, 3, "*"), 12),
        ((10, 2, "/"), 5.0),
        ((7, 7, "+"), 14),
    ]

    for args, expected in tests:
        result = calculate(*args)
        if result == expected:
            print(f"✅ Тест пройден: calculate{args} = {result}")
        else:
            print(f"❌ Тест НЕ пройден: calculate{args} ожидалось {expected}, получено {result}")


# ============================================
# ЗАПУСК ВСЕХ ТЕСТОВ
# ============================================

def run_all_tests():
    """Запустить все тесты"""
    print("="*60)
    print("🧪 ЗАПУСК ТЕСТОВ TDD LEVEL 0")
    print("="*60)

    test_greet()
    test_repeat_text()
    test_is_even()
    test_sum_to_n()
    test_build_stairs()
    test_calculate()

    print("\n" + "="*60)
    print("🏁 ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ")
    print("="*60)
    print("""
💡 СОВЕТЫ:
- Если тест ❌ не прошёл — прочитайте, что ожидалось
- Исправьте код и запустите снова
- Все тесты ✅ зелёные? Отлично, вы справились!
- Не смотрите решения сразу — попробуйте ещё раз!
    """)


# Запуск при выполнении файла
if __name__ == "__main__":
    run_all_tests()
