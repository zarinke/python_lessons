# Олимпиадные задачи по программированию

## 🏆 Что это такое?

Олимпиадные задачи — это задачи с соревнований по программированию:
- **LeetCode** — платформа с тысячами задач
- **Codeforces** — соревнования каждую неделю
- **Project Euler** — математические задачи

В этом файле — **3 задачи** от **Easy** до **Medium** уровня.

---

## 📋 Формат олимпиадных задач

Обычно задача состоит из:
1. **Описание** — что нужно сделать
2. **Примеры ввода/вывода**
3. **Ограничения** — размеры данных, время выполнения
4. **Подсказки** — направление решения (без готового ответа)

---

## 🎯 Критерии оценки решений

На олимпиадах важны:
- ✅ **Правильность** — проходит ли все тесты
- ⏱️ **Скорость работы** — O(n), O(n log n), O(n²)?
- 💾 **Память** — O(1), O(n)?
- 📝 **Читаемость кода** — понятно ли решение

---

# Задача 1: Two Sum (Сумма двух чисел)

## 📊 Метаданные
- **Источник:** LeetCode #1
- **Сложность:** Easy
- **Темы:** Массивы, Хэш-таблицы
- **Время решения:** 15-30 минут

## 📖 Описание

Дан массив целых чисел `nums` и целое число `target`.

Найдите **индексы двух чисел**, сумма которых равна `target`.

**Условия:**
- Каждый элемент используется только один раз
- Решение существует всегда
- Нельзя использовать один и тот же элемент дважды

## 💡 Примеры

### Пример 1:
```
Вход: nums = [2, 7, 11, 15], target = 9
Выход: [0, 1]
Объяснение: nums[0] + nums[1] = 2 + 7 = 9
```

### Пример 2:
```
Вход: nums = [3, 2, 4], target = 6
Выход: [1, 2]
Объяснение: nums[1] + nums[2] = 2 + 4 = 6
```

### Пример 3:
```
Вход: nums = [3, 3], target = 6
Выход: [0, 1]
```

## 🚫 Ограничения
- 2 ≤ len(nums) ≤ 10⁴
- -10⁹ ≤ nums[i] ≤ 10⁹
- -10⁹ ≤ target ≤ 10⁹
- Только одно решение существует

## 🧠 Подходы к решению

### Подход 1: Перебор (Brute Force)
- **Идея:** Проверить все пары чисел
- **Сложность:** O(n²) по времени, O(1) по памяти
- **Код:**
  ```python
  for i in range(len(nums)):
      for j in range(i+1, len(nums)):
          if nums[i] + nums[j] == target:
              return [i, j]
  ```
- **Проблема:** Медленно для больших массивов

### Подход 2: Хэш-таблица (Оптимальный)
- **Идея:** Для каждого числа проверить, есть ли в словаре `target - число`
- **Сложность:** O(n) по времени, O(n) по памяти
- **Алгоритм:**
  1. Создать пустой словарь `seen = {}`
  2. Для каждого числа и индекса:
     - Вычислить `complement = target - num`
     - Если `complement` в словаре: вернуть `[seen[complement], index]`
     - Иначе: добавить `seen[num] = index`
- **Это оптимальное решение!**

## 💡 Подсказки
1. Не обязательно искать числа — ищите ИНДЕКСЫ
2. Что если сохранять уже просмотренные числа и их индексы?
3. Для каждого числа `x` вы ищете `target - x`
4. Словарь (dict) позволяет проверять наличие элемента за O(1)

## 🎓 Что изучаете
- Работа с массивами
- Хэш-таблицы (словари в Python)
- Оптимизация алгоритмов
- Пространственно-временной компромисс

---

# Задача 2: Palindrome Number (Палиндром-число)

## 📊 Метаданные
- **Источник:** LeetCode #9
- **Сложность:** Easy
- **Темы:** Математика, Строки
- **Время решения:** 15-25 минут

## 📖 Описание

Определите, является ли целое число палиндромом.

Палиндром читается одинаково слева направо и справа налево.

**Важно:** Решите БЕЗ преобразования в строку (дополнительное задание).

## 💡 Примеры

### Пример 1:
```
Вход: x = 121
Выход: True
Объяснение: 121 читается как 121 слева направо и справа налево
```

### Пример 2:
```
Вход: x = -121
Выход: False
Объяснение: Слева направо: -121, справа налево: 121-
```

### Пример 3:
```
Вход: x = 10
Выход: False
Объяснение: Справа налево: 01 (не равно 10)
```

### Пример 4:
```
Вход: x = 0
Выход: True
```

## 🚫 Ограничения
- -2³¹ ≤ x ≤ 2³¹ - 1

## 🧠 Подходы к решению

### Подход 1: Через строку (Простой)
- **Идея:** Преобразовать число в строку и сравнить с перевёрнутой
- **Сложность:** O(n) по времени, O(n) по памяти
- **Код:**
  ```python
  if x < 0:
      return False
  s = str(x)
  return s == s[::-1]
  ```
- **Проблема:** Использует дополнительную память

### Подход 2: Математический (Оптимальный)
- **Идея:** Перевернуть число математически
- **Сложность:** O(log n) по времени, O(1) по памяти
- **Алгоритм:**
  1. Если x < 0: вернуть False (отрицательные не палиндромы)
  2. Если x заканчивается на 0 (но не равно 0): вернуть False
  3. Переворачиваем вторую половину:
     ```python
     reversed_half = 0
     while x > reversed_half:
         reversed_half = reversed_half * 10 + x % 10
         x //= 10
     return x == reversed_half or x == reversed_half // 10
     ```
  4. Сравниваем первую и вторую половины

## 💡 Подсказки
1. Отрицательные числа НЕ палиндромы
2. Числа, оканчивающиеся на 0 (кроме 0), НЕ палиндромы
3. Чтобы перевернуть число: `reversed = reversed * 10 + x % 10`, затем `x //= 10`
4. Можно переворачивать только половину числа
5. Для нечётного количества цифр: сравнить `x == reversed_half // 10`

## 🎓 Что изучаете
- Математические операции (%, //)
- Оптимизация памяти
- Работа с числами без преобразования в строки

---

# Задача 3: Climbing Stairs (Лестница)

## 📊 Метаданные
- **Источник:** LeetCode #70
- **Сложность:** Easy (но важная для понимания DP!)
- **Темы:** Динамическое программирование, Рекурсия
- **Время решения:** 20-35 минут

## 📖 Описание

Вы поднимаетесь по лестнице. Нужно `n` шагов, чтобы достичь вершины.

Каждый раз вы можете подняться на **1 шаг** или на **2 шага**.

**Вопрос:** Сколькими способами можно достичь вершины?

## 💡 Примеры

### Пример 1:
```
Вход: n = 2
Выход: 2
Объяснение:
1. 1 шаг + 1 шаг
2. 2 шага
```

### Пример 2:
```
Вход: n = 3
Выход: 3
Объяснение:
1. 1 + 1 + 1
2. 1 + 2
3. 2 + 1
```

### Пример 3:
```
Вход: n = 4
Выход: 5
Объяснение:
1. 1+1+1+1
2. 1+1+2
3. 1+2+1
4. 2+1+1
5. 2+2
```

## 🚫 Ограничения
- 1 ≤ n ≤ 45

## 🧠 Подходы к решению

### Подход 1: Рекурсия (Наивный)
- **Идея:** `climb(n) = climb(n-1) + climb(n-2)`
- **Сложность:** O(2ⁿ) — ОЧЕНЬ медленно!
- **Проблема:** Пересчитываем одни и те же значения

### Подход 2: Рекурсия с мемоизацией
- **Идея:** Сохранять уже вычисленные значения
- **Сложность:** O(n) по времени, O(n) по памяти
- **Код:**
  ```python
  def climb(n, memo={}):
      if n in memo:
          return memo[n]
      if n <= 2:
          return n
      memo[n] = climb(n-1, memo) + climb(n-2, memo)
      return memo[n]
  ```

### Подход 3: Динамическое программирование (Оптимальный)
- **Идея:** Заполнять таблицу снизу вверх
- **Сложность:** O(n) по времени, O(n) по памяти
- **Код:**
  ```python
  if n <= 2:
      return n
  dp = [0] * (n + 1)
  dp[1], dp[2] = 1, 2
  for i in range(3, n + 1):
      dp[i] = dp[i-1] + dp[i-2]
  return dp[n]
  ```

### Подход 4: Оптимизация памяти
- **Идея:** Хранить только последние 2 значения
- **Сложность:** O(n) по времени, O(1) по памяти
- **Код:**
  ```python
  if n <= 2:
      return n
  prev, curr = 1, 2
  for _ in range(3, n + 1):
      prev, curr = curr, prev + curr
  return curr
  ```

## 💡 Подсказки
1. Это на самом деле последовательность Фибоначчи!
2. `climb(1) = 1`, `climb(2) = 2`
3. `climb(n) = climb(n-1) + climb(n-2)`
4. Почему? На шаг `n` можно прийти либо с `n-1` (шаг 1), либо с `n-2` (шаг 2)
5. Начните с рекурсии, потом оптимизируйте через мемоизацию или DP

## 🎓 Что изучаете
- Динамическое программирование (DP)
- Мемоизация
- Оптимизация рекурсии
- Последовательность Фибоначчи
- Пространственная оптимизация

## 🔗 Связь с реальными задачами
- Подсчёт путей в лабиринте
- Комбинаторика
- Оптимизация маршрутов

---

# 🎯 Советы по решению олимпиадных задач

## Общий алгоритм:
1. **Прочитайте задачу 2-3 раза**
2. **Разберите примеры** — почему такой ответ?
3. **Нарисуйте** на бумаге
4. **Придумайте наивное решение** (пусть медленное)
5. **Напишите код** наивного решения
6. **Проверьте на примерах**
7. **Подумайте об оптимизации**
8. **Реализуйте оптимальное решение**

## Как думать об оптимизации:
- **O(n²) → O(n log n):** Попробуйте сортировку
- **O(n²) → O(n):** Попробуйте хэш-таблицу (словарь)
- **O(2ⁿ) → O(n):** Попробуйте мемоизацию или DP
- **Много повторных вычислений?** → Мемоизация
- **Порядок не важен?** → Сортировка может помочь

## Типичные паттерны:
- **Two Pointers** (два указателя)
- **Sliding Window** (скользящее окно)
- **Hash Map** (хэш-таблица)
- **Dynamic Programming** (динамическое программирование)
- **Greedy** (жадный алгоритм)
- **Binary Search** (бинарный поиск)
- **DFS/BFS** (поиск в глубину/ширину)

---

# 📚 Дополнительные ресурсы

## Платформы для практики:
1. **LeetCode** — https://leetcode.com
   - Начните с Easy задач
   - Фильтр по темам: Arrays, Strings, Hash Table
   - Есть обсуждения с решениями

2. **Codeforces** — https://codeforces.com
   - Соревнования каждую неделю
   - Архив задач (problemset)
   - Начните с Div. 2, задачи A и B

3. **HackerRank** — https://www.hackerrank.com
   - Структурированные треки
   - Хорошие объяснения

4. **AtCoder** — https://atcoder.jp
   - Beginner Contest каждую неделю
   - Очень качественные задачи

5. **Project Euler** — https://projecteuler.net
   - Математические задачи
   - Требуют больше математики, чем программирования

## Книги:
- **"Cracking the Coding Interview"** — Steven McDowell
- **"Introduction to Algorithms"** (CLRS) — классика
- **"Competitive Programming"** — Steven & Felix Halim

## YouTube каналы:
- **NeetCode** — разбор LeetCode задач
- **Back To Back SWE**
- **William Fiset** — алгоритмы и структуры данных

---

# ⚡ Следующие шаги

После решения этих задач:

1. **Решайте по 1-2 задачи в день** на LeetCode
2. **Участвуйте в соревнованиях** (Codeforces, AtCoder)
3. **Изучайте алгоритмы**: сортировки, графы, деревья
4. **Читайте чужие решения** — учитесь у лучших
5. **Объясняйте решения** другим — лучший способ закрепить

---

**Удачи в олимпиадах!** 🚀

*Помните: даже топовые программисты когда-то начинали с Easy задач. Главное — постоянная практика!*
