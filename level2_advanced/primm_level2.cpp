#include <iostream>
#include <string>
#include <map>
#include <vector>
using namespace std;

// ============================================
// LEVEL 2: PRIMM ЗАДАЧИ (ADVANCED) - C++
// ============================================
// Методология: Predict → Run → Investigate → Modify → Make
// Время выполнения: 60-70 минут
// ============================================

/*
🎯 PRIMM НА C++

Это те же задачи, что и в primm_level2.py, но на C++!

Увидите разницу:
- Строгая типизация (int, string, etc.)
- Компиляция перед запуском
- Заголовочные файлы (#include)
- Точки с запятой обязательны

💻 КАК ЗАПУСТИТЬ:
Mac/Linux:
    g++ -o program primm_level2.cpp
    ./program

Windows:
    g++ -o program.exe primm_level2.cpp
    program.exe

Online:
    https://www.onlinegdb.com/online_c++_compiler
*/

void separator(const string& text) {
    cout << string(60, '=') << endl;
    cout << text << endl;
    cout << string(60, '=') << endl;
}

// ============================================
// ЗАДАЧА 1: ФУНКЦИИ В C++
// ============================================

void task1_functions() {
    separator("ЗАДАЧА 1: ФУНКЦИИ В C++");

    // ============================================
    // P - PREDICT (Предсказать)
    // ============================================

    const string код_для_анализа = R"(
int calculateArea(int width, int height) {
    int area = width * height;
    return area;
}

int main() {
    int result = calculateArea(5, 3);
    cout << result << endl;
    return 0;
}
)";

    cout << "\n📋 КОД ДЛЯ АНАЛИЗА:\n" << код_для_анализа << endl;
    cout << "❓ Что выведется? Запишите ваш ответ!\n" << endl;
    cout << "⏸️  ОСТАНОВИТЕСЬ ЗДЕСЬ! Сделайте предсказание!\n" << endl;

    // ============================================
    // R - RUN (Запустить)
    // ============================================
    separator("R - ЗАПУСК КОДА");

    cout << "Раскомментируйте функцию ниже и запустите программу:\n";
    cout << "/*\n";
    cout << "int calculateArea(int width, int height) {\n";
    cout << "    int area = width * height;\n";
    cout << "    return area;\n";
    cout << "}\n";
    cout << "// Добавьте вызов в main(): cout << calculateArea(5, 3) << endl;\n";
    cout << "*/\n" << endl;

    // Для демонстрации - раскомментируйте:
    // int result = calculateArea(5, 3);
    // cout << result << endl;

    cout << "✅ Совпало с вашим предсказанием?\n" << endl;

    // ============================================
    // I - INVESTIGATE (Исследовать)
    // ============================================
    separator("I - ИССЛЕДОВАНИЕ");

    cout << R"(
📚 СИНТАКСИС ФУНКЦИЙ В C++:

возвращаемый_тип имяФункции(тип параметр1, тип параметр2) {
    // код
    return результат;
}

🔍 РАЗБОР:
• int, string, void  — тип возвращаемого значения
• имяФункции         — имя функции
• (int x, int y)     — параметры с типами
• return результат   — возврат значения
• void               — функция ничего не возвращает

🆚 ОТЛИЧИЯ ОТ PYTHON:
Python                      C++
def func(x):               int func(int x) {
    return x * 2               return x * 2;
                           }
print(x)                   cout << x << endl;
# комментарий             // комментарий
не нужны типы            нужны типы везде

❓ ВОПРОСЫ ДЛЯ РАЗМЫШЛЕНИЯ:
1. Зачем нужны типы параметров?
2. Что будет, если не указать return в int-функции?
3. Можно ли вызвать функцию до её объявления?
)" << endl;

    // ============================================
    // M - MODIFY (Модифицировать)
    // ============================================
    separator("M - МОДИФИКАЦИЯ");

    cout << R"(
🎯 ЗАДАНИЕ 1: Функция приветствия
Создайте функцию greet(string name), которая:
- Принимает имя
- Возвращает строку "Привет, [имя]!"

Шаблон:
string greet(string name) {
    // ваш код
}
)" << endl;

    cout << "--- Ваше решение (задание 1) ---" << endl;
    cout << "// Напишите функцию здесь:\n\n\n" << endl;

    cout << R"(
🎯 ЗАДАНИЕ 2: Функция максимума
Создайте функцию max(int a, int b), которая возвращает большее число.

💡 ПОДСКАЗКА: используйте if-else
)" << endl;

    cout << "--- Ваше решение (задание 2) ---" << endl;
    cout << "// Напишите функцию здесь:\n\n\n" << endl;

    // ============================================
    // M - MAKE (Создать)
    // ============================================
    separator("M - СОЗДАНИЕ СВОЕГО КОДА");

    cout << R"(
🎯 ТВОРЧЕСКОЕ ЗАДАНИЕ:
Создайте функцию calculateDiscount(double price, int percent), которая:
1. Принимает цену товара и процент скидки
2. Вычисляет сумму скидки
3. Возвращает новую цену

Пример:
calculateDiscount(100.0, 20) → 80.0  (скидка 20%)
calculateDiscount(50.0, 10)  → 45.0  (скидка 10%)

💡 ПОДСКАЗКИ:
- Используйте тип double для цены
- Формула скидки: price - (price * percent / 100)
- Не забудьте точку с запятой!
)" << endl;

    cout << "--- Ваше творческое решение ---" << endl;
    cout << "// Напишите функцию здесь:\n\n\n" << endl;
}

// ============================================
// ЗАДАЧА 2: СЛОВАРИ (MAP) В C++
// ============================================

void task2_dictionaries() {
    separator("ЗАДАЧА 2: СЛОВАРИ (MAP) В C++");

    // ============================================
    // P - PREDICT (Предсказать)
    // ============================================

    const string код_для_анализа = R"(
map<string, int> ages;
ages["Алиса"] = 25;
ages["Боб"] = 30;
ages["Чарли"] = 22;

cout << "Возраст Боба: " << ages["Боб"] << endl;
cout << "Количество людей: " << ages.size() << endl;
)";

    cout << "\n📋 КОД ДЛЯ АНАЛИЗА:\n" << код_для_анализа << endl;
    cout << "❓ Что выведется? Запишите ваш ответ!\n" << endl;
    cout << "⏸️  ОСТАНОВИТЕСЬ ЗДЕСЬ! Сделайте предсказание!\n" << endl;

    // ============================================
    // R - RUN (Запустить)
    // ============================================
    separator("R - ЗАПУСК КОДА");

    cout << "Раскомментируйте код ниже:\n";
    cout << "/*\n";
    cout << "map<string, int> ages;\n";
    cout << "ages[\"Алиса\"] = 25;\n";
    cout << "ages[\"Боб\"] = 30;\n";
    cout << "cout << ages[\"Боб\"] << endl;\n";
    cout << "*/\n" << endl;

    // Для демонстрации:
    map<string, int> ages;
    ages["Алиса"] = 25;
    ages["Боб"] = 30;
    ages["Чарли"] = 22;

    cout << "Возраст Боба: " << ages["Боб"] << endl;
    cout << "Количество людей: " << ages.size() << endl;
    cout << "\n✅ Совпало с вашим предсказанием?\n" << endl;

    // ============================================
    // I - INVESTIGATE (Исследовать)
    // ============================================
    separator("I - ИССЛЕДОВАНИЕ");

    cout << R"(
📚 СИНТАКСИС MAP (СЛОВАРЬ) В C++:

map<тип_ключа, тип_значения> имя;
имя[ключ] = значение;

🔍 РАЗБОР:
• map<string, int>  — словарь где ключ=строка, значение=число
• ages["Боб"] = 30  — добавить пару ключ-значение
• ages["Боб"]       — получить значение по ключу
• ages.size()       — количество элементов
• ages.count(ключ)  — проверка наличия ключа (0 или 1)

🆚 ОТЛИЧИЯ ОТ PYTHON:
Python                  C++
ages = {}              map<string, int> ages;
ages["Боб"] = 30       ages["Боб"] = 30;
len(ages)              ages.size()
"Боб" in ages          ages.count("Боб") > 0

❓ ВОПРОСЫ:
1. Что будет, если обратиться к несуществующему ключу?
2. Можно ли использовать int как ключ?
3. Как перебрать все элементы map?
)" << endl;

    // ============================================
    // M - MODIFY (Модифицировать)
    // ============================================
    separator("M - МОДИФИКАЦИЯ");

    cout << R"(
🎯 ЗАДАНИЕ 3: Добавьте новый элемент
Дополните код:
- Добавьте ещё одного человека "Дэйв" с возрастом 28
- Выведите его возраст
)" << endl;

    cout << "--- Ваше решение (задание 3) ---" << endl;
    cout << "// Напишите код здесь:\n\n\n" << endl;

    // ============================================
    // M - MAKE (Создать)
    // ============================================
    separator("M - СОЗДАНИЕ СВОЕГО КОДА");

    cout << R"(
🎯 ТВОРЧЕСКОЕ ЗАДАНИЕ:
Создайте телефонную книгу:
1. Создайте map<string, string> для хранения имён и телефонов
2. Добавьте 3-4 контакта
3. Выведите телефон одного из контактов
4. Выведите общее количество контактов

Пример вывода:
Телефон Алисы: +7-123-456-78-90
Всего контактов: 4
)" << endl;

    cout << "--- Ваше творческое решение ---" << endl;
    cout << "// Напишите код здесь:\n\n\n" << endl;
}

// ============================================
// ЗАДАЧА 3: ВЕКТОРЫ (СПИСКИ) В C++
// ============================================

void task3_vectors() {
    separator("ЗАДАЧА 3: ВЕКТОРЫ (СПИСКИ) В C++");

    // ============================================
    // P - PREDICT (Предсказать)
    // ============================================

    const string код_для_анализа = R"(
vector<int> numbers = {10, 20, 30, 40, 50};

numbers.push_back(60);
cout << "Размер: " << numbers.size() << endl;
cout << "Первый элемент: " << numbers[0] << endl;
cout << "Последний элемент: " << numbers.back() << endl;
)";

    cout << "\n📋 КОД ДЛЯ АНАЛИЗА:\n" << код_для_анализа << endl;
    cout << "❓ Что выведется? Запишите ваш ответ!\n" << endl;
    cout << "⏸️  ОСТАНОВИТЕСЬ ЗДЕСЬ! Сделайте предсказание!\n" << endl;

    // ============================================
    // R - RUN (Запустить)
    // ============================================
    separator("R - ЗАПУСК КОДА");

    vector<int> numbers = {10, 20, 30, 40, 50};
    numbers.push_back(60);

    cout << "Размер: " << numbers.size() << endl;
    cout << "Первый элемент: " << numbers[0] << endl;
    cout << "Последний элемент: " << numbers.back() << endl;
    cout << "\n✅ Совпало с вашим предсказанием?\n" << endl;

    // ============================================
    // I - INVESTIGATE (Исследовать)
    // ============================================
    separator("I - ИССЛЕДОВАНИЕ");

    cout << R"(
📚 СИНТАКСИС VECTOR В C++:

vector<тип> имя = {элемент1, элемент2, ...};

🔍 ОСНОВНЫЕ МЕТОДЫ:
• push_back(x)   — добавить элемент в конец
• pop_back()     — удалить последний элемент
• size()         — количество элементов
• [index]        — доступ по индексу
• back()         — последний элемент
• front()        — первый элемент
• clear()        — очистить вектор

🆚 ОТЛИЧИЯ ОТ PYTHON:
Python                 C++
nums = [1, 2, 3]      vector<int> nums = {1, 2, 3};
nums.append(4)        nums.push_back(4);
len(nums)             nums.size()
nums[-1]              nums.back()

❓ ВОПРОСЫ:
1. В чём разница между vector и массив?
2. Можно ли хранить разные типы в одном vector?
)" << endl;

    // ============================================
    // M - MODIFY (Модифицировать)
    // ============================================
    separator("M - МОДИФИКАЦИЯ");

    cout << R"(
🎯 ЗАДАНИЕ 4: Модифицируйте вектор
Создайте vector с числами {5, 10, 15}:
1. Добавьте число 20
2. Добавьте число 25
3. Выведите размер вектора
4. Выведите все элементы
)" << endl;

    cout << "--- Ваше решение (задание 4) ---" << endl;
    cout << "// Напишите код здесь:\n\n\n" << endl;

    // ============================================
    // M - MAKE (Создать)
    // ============================================
    separator("M - СОЗДАНИЕ СВОЕГО КОДА");

    cout << R"(
🎯 ТВОРЧЕСКОЕ ЗАДАНИЕ:
Создайте программу "Список покупок":
1. Создайте vector<string> для названий товаров
2. Создайте vector<double> для цен товаров
3. Добавьте 3-4 товара с ценами
4. Вычислите и выведите общую сумму
5. Выведите самый дорогой товар

Пример вывода:
Товаров: 4
Общая сумма: 250.50
Самый дорогой: Молоко (100.00)
)" << endl;

    cout << "--- Ваше творческое решение ---" << endl;
    cout << "// Напишите код здесь:\n\n\n" << endl;
}

// ============================================
// MAIN ФУНКЦИЯ
// ============================================

int main() {
    cout << R"(
============================================
PRIMM ЗАДАЧИ - C++ LEVEL 2
============================================

Выполните задачи последовательно:
1. Функции в C++
2. Словари (map)
3. Векторы (списки)

Для каждой задачи:
✅ Predict — предскажите результат
✅ Run — запустите код
✅ Investigate — изучите синтаксис
✅ Modify — модифицируйте код
✅ Make — создайте свой код

Раскомментируйте функции ниже по очереди:
)" << endl;

    // Раскомментируйте по одной:
    // task1_functions();
    // task2_dictionaries();
    // task3_vectors();

    cout << "\n============================================" << endl;
    cout << "🎉 PRIMM ЗАДАЧИ C++ ЗАВЕРШЕНЫ!" << endl;
    cout << "============================================" << endl;
    cout << "\n📍 Следующий шаг: test_driven_level2.cpp" << endl;

    return 0;
}
