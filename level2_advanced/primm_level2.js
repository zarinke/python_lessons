// ============================================
// LEVEL 2: PRIMM ЗАДАЧИ (ADVANCED) - JAVASCRIPT
// ============================================
// Методология: Predict → Run → Investigate → Modify → Make
// Время выполнения: 50-60 минут
// ============================================

/*
🎯 PRIMM НА JAVASCRIPT

Это те же задачи, что и в primm_level2.py, но на JavaScript!

Увидите разницу:
- Синтаксис (let/const, {}, точки с запятой)
- Типизация (слабая в JS vs Python)
- Методы (console.log vs print)

💻 КАК ЗАПУСТИТЬ:
1. Node.js: node primm_level2.js
2. Браузер: F12 → Console → вставить код
3. Online: jsfiddle.net, codepen.io, runjs.app

Рекомендуем Node.js!
*/

console.log("=".repeat(60));
console.log("ЗАДАЧА 1: ФУНКЦИИ В JAVASCRIPT");
console.log("=".repeat(60));

// ============================================
// P - PREDICT (Предсказать)
// ============================================

const кодДляАнализа = `
function calculateArea(width, height) {
    const area = width * height;
    return area;
}

const result = calculateArea(5, 3);
console.log(result);
`;

console.log(`\n📋 КОД ДЛЯ АНАЛИЗА:\n${кодДляАнализа}`);
console.log("❓ Что выведется? Запишите ваш ответ!\n");

// ⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ! Сделайте предсказание!

// ============================================
// R - RUN (Запустить)
// ============================================
console.log("=".repeat(60));
console.log("R - ЗАПУСК КОДА");
console.log("=".repeat(60));

// Раскомментируйте:
// function calculateArea(width, height) {
//     const area = width * height;
//     return area;
// }
//
// const result = calculateArea(5, 3);
// console.log(result);

console.log("\n✅ Совпало с вашим предсказанием?\n");

// ============================================
// I - INVESTIGATE (Исследовать)
// ============================================
console.log("=".repeat(60));
console.log("I - ИССЛЕДОВАНИЕ");
console.log("=".repeat(60));

console.log(`
📚 СИНТАКСИС ФУНКЦИЙ В JAVASCRIPT:

// Способ 1: Function Declaration
function имяФункции(параметр1, параметр2) {
    // код
    return результат;
}

// Способ 2: Function Expression
const имяФункции = function(параметр1, параметр2) {
    // код
    return результат;
};

// Способ 3: Arrow Function (современный)
const имяФункции = (параметр1, параметр2) => {
    // код
    return результат;
};

// Короткая arrow function (автоматический return)
const имяФункции = (параметр1, параметр2) => результат;

🔍 РАЗБОР:
• function          — ключевое слово
• const/let         — объявление переменной
• =>                — стрелочная функция (ES6)
• return            — возврат значения
• ;                 — точка с запятой (опциональна, но рекомендуется)

💡 ВАЖНО:
• let — изменяемая переменная
• const — неизменяемая (рекомендуется для функций)
• var — старый способ (не используйте!)
• {} — обязательные фигурные скобки для блоков

🆚 ОТЛИЧИЯ ОТ PYTHON:
Python                  JavaScript
def func():            function func() {}
def func():            const func = () => {}
x = 5                  let x = 5; или const x = 5;
print(x)               console.log(x);
# комментарий          // комментарий
""" многострочный """  /* многострочный */
`);

// ============================================
// M - MODIFY (Модифицировать)
// ============================================
console.log("=".repeat(60));
console.log("M - МОДИФИКАЦИЯ");
console.log("=".repeat(60));

console.log(`
🎯 ЗАДАНИЕ 1: Функция приветствия

Создайте функцию greet(name), которая:
- Принимает имя
- Возвращает строку "Привет, [имя]!"

Пример:
greet("Алиса") → "Привет, Алиса!"

💡 ТРИ СПОСОБА:
// Способ 1
function greet(name) {
    return "Привет, " + name + "!";
}

// Способ 2
const greet = function(name) {
    return \`Привет, \${name}!\`;  // template literal
};

// Способ 3 (самый короткий)
const greet = (name) => \`Привет, \${name}!\`;
`);

console.log("\n--- Ваше решение (задание 1) ---");
// Напишите код здесь:




console.log(`
🎯 ЗАДАНИЕ 2: Конвертер температуры

Создайте две функции:
1. celsiusToFahrenheit(c) — C → F
2. fahrenheitToCelsius(f) — F → C

Формулы:
F = C * 9/5 + 32
C = (F - 32) * 5/9
`);

console.log("\n--- Ваше решение (задание 2) ---");
// Напишите код здесь:




// ============================================
// ЗАДАЧА 2: ОБЪЕКТЫ (АНАЛОГ СЛОВАРЕЙ PYTHON)
// ============================================
console.log("\n" + "=".repeat(60));
console.log("ЗАДАЧА 2: ОБЪЕКТЫ В JAVASCRIPT");
console.log("=".repeat(60));

// P - PREDICT
const кодДляАнализа2 = `
const person = {
    имя: "Алиса",
    возраст: 15,
    город: "Москва"
};

console.log(person.имя);
console.log(person["возраст"]);
person.возраст = 16;
person.хобби = "программирование";
console.log(person);
`;

console.log(`\n📋 КОД:\n${кодДляАнализа2}`);
console.log("❓ Что выведется?\n");

// R - RUN
console.log("--- ЗАПУСК ---");
// Раскомментируйте:
// const person = {
//     имя: "Алиса",
//     возраст: 15,
//     город: "Москва"
// };
//
// console.log(person.имя);
// console.log(person["возраст"]);
// person.возраст = 16;
// person.хобби = "программирование";
// console.log(person);

// I - INVESTIGATE
console.log(`
📚 СИНТАКСИС ОБЪЕКТОВ:

Создание:
const объект = {
    ключ1: "значение1",
    ключ2: "значение2"
};

Доступ:
• объект.ключ         — через точку (рекомендуется)
• объект["ключ"]      — через квадратные скобки

Операции:
• объект.ключ = значение     — добавить/изменить
• delete объект.ключ         — удалить
• "ключ" in объект           — проверка существования
• Object.keys(объект)        — все ключи
• Object.values(объект)      — все значения
• Object.entries(объект)     — пары [ключ, значение]

🆚 ОТЛИЧИЯ ОТ PYTHON:
Python                      JavaScript
dict = {"key": "val"}      const obj = {key: "val"};
dict["key"]                obj.key или obj["key"]
dict.get("key", default)   obj.key || default
dict.keys()                Object.keys(obj)
for k, v in dict.items()   for (let [k, v] of Object.entries(obj))
`);

// M - MODIFY
console.log(`
🎯 ЗАДАНИЕ 3: Телефонная книга

Создайте объект contacts с минимум 3 контактами.
Выполните операции:
1. Вывести номер одного контакта
2. Добавить новый контакт
3. Изменить существующий номер
4. Вывести весь объект
`);

console.log("\n--- Ваше решение (задание 3) ---");
// Напишите код здесь:




// ============================================
// ЗАДАЧА 3: РЕКУРСИЯ В JAVASCRIPT
// ============================================
console.log("\n" + "=".repeat(60));
console.log("ЗАДАЧА 3: РЕКУРСИЯ");
console.log("=".repeat(60));

console.log(`
🎯 ЗАДАНИЕ 4: Факториал (рекурсия)

Создайте рекурсивную функцию factorial(n):

function factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

console.log(factorial(5));  // 120
`);

console.log("\n--- Ваше решение (задание 4) ---");
// Напишите код здесь:




console.log(`
🎯 ЗАДАНИЕ 5: Числа Фибоначчи (рекурсия с мемоизацией)

Создайте функцию fibonacci с мемоизацией:

const fibonacci = (function() {
    const memo = {};

    return function fib(n) {
        if (n in memo) return memo[n];
        if (n <= 1) return n;
        memo[n] = fib(n - 1) + fib(n - 2);
        return memo[n];
    };
})();

console.log(fibonacci(10));  // 55
`);

console.log("\n--- Ваше решение (задание 5) ---");
// Напишите код здесь:




// ============================================
// ИТОГОВОЕ ЗАДАНИЕ
// ============================================
console.log("\n" + "=".repeat(60));
console.log("🏆 ИТОГОВОЕ ЗАДАНИЕ");
console.log("=".repeat(60));

console.log(`
Создайте программу "Библиотека книг" на JavaScript:

1. Создайте объект books:
const books = {
    "1984": {автор: "Оруэлл", год: 1949, прочитана: true},
    "Война и мир": {автор: "Толстой", год: 1869, прочитана: false}
};

2. Создайте функции:

function addBook(books, title, author, year, read) {
    books[title] = {автор: author, год: year, прочитана: read};
}

function markAsRead(books, title) {
    if (title in books) {
        books[title].прочитана = true;
    }
}

function getUnreadBooks(books) {
    return Object.keys(books).filter(title => !books[title].прочитана);
}

function getStatistics(books) {
    const total = Object.keys(books).length;
    const read = Object.values(books).filter(b => b.прочитана).length;
    const unread = total - read;
    const authors = [...new Set(Object.values(books).map(b => b.автор))];

    return {
        всегоКниг: total,
        прочитано: read,
        неПрочитано: unread,
        авторы: authors
    };
}

3. Протестируйте все функции!
`);

console.log("\n--- Ваше финальное решение ---");
// Напишите код здесь:




// ============================================
// ПРОВЕРКА ЗНАНИЙ
// ============================================
console.log("\n" + "=".repeat(60));
console.log("✅ ПРОВЕРКА ЗНАНИЙ");
console.log("=".repeat(60));

console.log(`
Если вы можете ответить на эти вопросы — вы знаете JavaScript!

1. В чём разница между let, const и var?
2. Что такое arrow function?
3. В чём разница между obj.key и obj["key"]?
4. Как работает замыкание (closure) в JavaScript?
5. Что такое template literal?
6. Как перебрать объект (аналог dict.items() в Python)?
7. В чём разница между == и ===?
8. Что такое hoisting?

🔗 Полезные ресурсы:
- MDN Web Docs: https://developer.mozilla.org/ru/docs/Web/JavaScript
- JavaScript.info: https://javascript.info
- Eloquent JavaScript: https://eloquentjavascript.net
`);

console.log("\n" + "=".repeat(60));
console.log("🎉 PRIMM ЗАДАЧИ LEVEL 2 (JavaScript) ЗАВЕРШЕНЫ!");
console.log("=".repeat(60));
console.log(`
📍 Следующие шаги:
- test_driven_level2.js (TDD задачи на JS)
- Попробуйте primm_level2.cpp (C++)
- Решайте задачи на LeetCode в JavaScript
- Изучайте современный JS (ES6+)
`);
